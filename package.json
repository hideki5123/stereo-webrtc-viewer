{
  "name": "stereo-camera-viewer",
  "version": "1.0.0",
  "description": "Web-based stereoscopic camera viewer using Sora WebRTC and WebXR for VR",
  "main": "dist/index.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/webxr": "^0.5.22",
    "typescript": "^5.1.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "sora-js-sdk": "^2024.2.2"
  }
}
